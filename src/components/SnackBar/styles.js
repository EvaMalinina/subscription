const styles = ((theme) => ({
  wrapper: {
    margin: '0 auto 35px',
    width: '30%',
    minWidth: '300px',
    padding: theme.spacing(2),
  },
  title: {
    fontSize: 14,
    marginBottom: '15px'
  },
  subtitle: {
    fontSize: 12,
    marginBottom: '10px'
  },
  discount: {
    color: 'red',
  }
}));

export default styles;